# -*- coding: utf-8 -*-
# vim: ft=yaml
---
strongswan:
  pkg: strongswan
  config:
    global_options: /etc/ipsec.conf
    global_secrets: /etc/ipsec.secrets
    dropin_options: /etc/ipsec.conf.d
#    secrets_dropin: /etc
  service:
    name: strongswan
